class Battery {
    public int capacityMah; // Capacity in milliampere-hours
    public boolean fastChargingSupport;

    public Battery(int capacityMah, boolean fastChargingSupport) {
        this.capacityMah = capacityMah;
        this.fastChargingSupport = fastChargingSupport;
    }
}